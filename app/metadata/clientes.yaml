clientes:
  config:
    source: api
    layer: bronze
    description: Person data from CRM

  columns:

    cod_cliente:      
      role: bk
      hash_key: hk_cliente
      dtype: int64
      nullable: false
      description: Primary key of person

    cod_endereco:      
      role: fk
      hash_key: hk_endereco
      dtype: int64
      nullable: false
      description: Primary key of endereco

    cod_contato:
      role: fk
      hash_key: hk_contato
      dtype: int64
      nullable: false
      description: Foreign key of contact

    data_primeira_compra:
      role: attribute
      dtype: string
      nullable: false      
      description: description

    data_criacao:
      role: metadata
      dtype: datetime64[ns]
      nullable: false
      description: Load timestamp

    data_alteracao:
      role: metadata
      dtype: datetime64[ns]
      nullable: false
      description: Load timestamp